# Bookie - Terminal Book Manager

## How to use

- Book covers are expected in folder `./covers` alongside the app.
- The app will also create a save file `bookie-state.json`, and a log file `bookie.log`.
